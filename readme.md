# Lit-Closure!

this is repo which build and serve lit-element via google closure compiler

its require custom build of lit repository, check the package.json

the repo with changes: https://github.com/moczix/lit/tree/main-closure2

1. clone
2. build :

- `yarn boostrap`
- `yarn build`

3. it maybe issues about `module` type in package.json
